{
    "model": "vgg11",
    "input_info": {
      "sample_size": [1, 3, 224, 224]
    },
    "num_classes": 1000,
    "batch_size" : 128,
    "workers": 8,
    "compression": {
        "algorithm": "quantization",
        "initializer": {
            "type": "min_max",
            "num_init_steps": 1
        },
        "weights":     { "mode": "asymmetric", "bits": 8, "per_channel": false },
        "activations": { "mode": "asymmetric", "bits": 8, "per_channel": false }
    }
}